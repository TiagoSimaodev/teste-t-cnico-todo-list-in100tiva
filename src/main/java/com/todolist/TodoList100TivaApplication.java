package com.todolist;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Classe principal da aplicação To-Do List.
 * Esta classe inicia o contexto do Spring Boot e configura a aplicação.
 */
@SpringBootApplication
public class TodoList100TivaApplication {

    public static void main(String[] args) {
        SpringApplication.run(TodoList100TivaApplication.class, args);
    }

}